- TASK 0 — Repo discovery (NO CODE) - concluída em 2026-03-03
- Observações: Stack HTTP inexistente no estado atual (sem WebServer/AsyncWebServer), build system confirmado como PlatformIO/Arduino via `platformio.ini`, `pio run` executado com sucesso e abordagem definida para próximas tasks: `WebServer` + `Update.h`.
- TASK 1 — Add OTA config field (NO OTA flashing yet) - concluída em 2026-03-03
- Observações: Adicionado `otaKey` em `DeviceConfig` com `DEFAULT_OTA_KEY`, atualizado `setDefaultConfig`, persistência JSON (`ota_key`) em `saveConfig` e leitura opcional em `applyConfigFromJson` mantendo retrocompatibilidade para `/config.json` antigo sem `ota_key`; build validada com `pio run`.
- TASK 2 — Add minimal HTTP server module (endpoint skeleton) - concluída em 2026-03-03
- Observações: Criado módulo `src/ota/OtaServer.*` com `WebServer` e integração no `main.cpp`; `POST /ota` retorna JSON determinístico com 501 (`not_implemented`) e `GET /ota/status` retorna conteúdo de `/ota/last.json` quando existir, senão `{\"ok\":false,\"message\":\"no status\"}`; `loopOtaServer()` foi adicionado ao loop principal; build validada com `pio run`.
- TASK 3 — Implement OTA flashing via HTTP POST streaming (Update.h) - concluída em 2026-03-03 13:06
- Observações: `POST /ota` implementado com `server.on(..., uploadHandler)` e `Update.write` por chunks, autenticação obrigatória via header `X-OTA-Key` (rejeita `ota_key` vazio e chave inválida), validação de `Content-Length` quando presente e `Update.begin(...)` correspondente; em erro de OTA o fluxo executa `Update.abort()` e retorna JSON 4xx/5xx sem reboot, enquanto sucesso finaliza com `Update.end(true)`, responde `{\"ok\":true,\"message\":\"updated\",\"bytes_written\":n}` e reinicia após pequeno delay; build validada com `pio run`.
