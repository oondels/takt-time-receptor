- Detectar mensagem update_takt_time via MQTT - concluída em 2026-03-03 16:05
- Observações: No callback MQTT em `src/main.cpp`, foi adicionada detecção de `event` ou `message` igual a `update_takt_time`, validação de `update_url` (não vazio e com prefixo `http://` ou `https://`) e sinalização assíncrona com `otaPending`, `pendingUpdateUrl` e `pendingUpdateTimestamp`; em caso de URL inválida, registra erro e encerra sem travar. Build validado com `pio run` concluído com sucesso.
- Implementar módulo OTA via HTTP (cliente) - concluída em 2026-03-03 16:07
- Observações: Criados `src/ota/OtaMqttTrigger.h` e `src/ota/OtaMqttTrigger.cpp` com `triggerOtaFromUrl(DeviceConfig& cfg, const String& updateUrl)` usando `HTTPClient` + `WiFiClient`, download em stream com buffer de 1KB, `Update.begin`, `Update.write` e `Update.end(true)`. Em falhas (`HTTP != 200`, timeout de stream, erro de escrita/finalização), executa `Update.abort()` e persiste `/ota/last.json` com `result=fail`, `error` e `update_url`; em sucesso persiste `result=ok` com bytes e tempos.
- Agendar execução OTA fora do callback MQTT - concluída em 2026-03-03 16:08
- Observações: Integrado `triggerOtaFromUrl()` no `loop()` de `src/main.cpp` (fora do callback MQTT), com verificação de `otaPending` e proteção single-flight via `otaInProgress`. Ao disparar, a flag pendente é consumida uma única vez, a OTA é executada com Wi-Fi conectado e, em sucesso, o dispositivo reinicia; em falha, registra log e continua operando sem travar reconexão MQTT.
- Criar servidor Node.js minimalista para firmware - concluída em 2026-03-03 16:10
- Observações: Criados `tools/ota-server/server.js` e `tools/ota-server/package.json` com servidor HTTP sem dependências externas. Implementado `GET /update-takttime` retornando `firmware.bin` com `Content-Type: application/octet-stream` e `Content-Length` via `fs.stat`, além de `PORT` e `FIRMWARE_PATH` por variáveis de ambiente. Validação executada com `node server.js` + `curl` (porta alternativa `2499`, devido `2399` ocupada no ambiente), recebendo `HTTP 200` e download binário com tamanho consistente.
